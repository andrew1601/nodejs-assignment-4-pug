extends layouts/main.pug

block styles
    link(rel="stylesheet", href="/css/users.css")

block main
    h1 User List
    if users.length > 0
        ul.users-list
            for user in users
                li.users-list__item #{user.name}
    else
        h3 No users :(